#!/bin/bash

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm

nvm use --lts # preset nodeswitcher

if [ -x $(command -v codium) ]; then
    exec codium /home/jan/.local/share/chezmoi
else
    exec code /home/jan/.local/share/chezmoi
fi
