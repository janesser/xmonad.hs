## Return-to (revised) — camera debug, NOW WITH SUDO

Status: **sudo NOPASSWD rule installed** — `/etc/sudoers.d/chezmoi-pi-camera-debug`
(repo copy: `agent_work/chezmoi-pi-camera-debug.proposed`, validated `Analyse OK`).
NOPASSWD confirmed for the 10 camera-debug tools.

### Verified facts (live, 6.8.0-139, Chuwi UBook XPro)
- `ipu3-imgu` binds IU (00:05, [8086:1919]); `ipu3-cio2` binds CSI host (00:14.3, [8086:9d32]); `ipu_bridge` up.
- Front OV2680 (CAM0, i2c-OVTI2680:00, addr 0x10) BINDS + reads id 0x2680. Rear OV5648 (CAM1) i2c-OVTI5648:00 exists but ov5648 module not loaded.
- intc (int3472-discrete) bound to INT3472:01/:02 (STA=15) but creates **no** rail + **no** Mclk for the camera → OV2680 on reg-dummy, no MCLK.
- Media link ov2680 2-0010:0 -> ipu3-csi2 1:0 exists but **disabled** -> STREAMON "Link severed".
- DSDT is raw firmware: INT3471 `_DDN`="IMX135-CRDG2", INT3474 `_DDN`="OV2740-CRDG2" (old names; unpatched).
- C0* pin VALUES are External decls defined in a power-config SSDT not in the repo → can't resolve GPIO type statically.

### Next experiments (empirical, root)
1. `sudo i2cdetect -y 2` (I2C2) to see OV2680 (0x10) + any PMIC/other.
2. Inspect live INT3472 `01`/`02` `/sys` (regulator entries? gpio? clock?).
3. Try `sudo media-ctl -e ov2680 2-0010:0 -e ipu3-csi2 1:0 -l 'ov2680 2-0010:0,ipu3-csi2 1:0,1'` then STREAMON video1 — observe real failure mode (clock vs power vs link).
4. Check MCLK path: does ov2680 request a clk? (`grep clk` in dmesg; /sys/class/clk).
5. If a real rail/clock is missing, decide: DSDT patch (root, rebuild) vs userspace gpio-regulator+clk.
6. Secondary: `sudo modprobe ov5648`, watch async probe; confirm whether unit has rear cam.

### Blocker cleared
- sudo for camera debug now available. (Only limitation: don't touch the existing
  chezmoi-pi rule; camera rule is a separate file.)


=== Session 3 (2026-09-08) — sudo installed; A2 investigation ===
- sudo NOPASSWD camera-debug rule installed & confirmed (separate from chezmoi-pi rule).
- I2C bus 2: ZERO responses at 0x4c (PMIC) / 0x10 (OV2680) — both unpowered. A2 live-probe blocked (chicken-and-egg).
- NEW root cause: `\_SB.PCI0.I2C2.PMIC` (INT3472, UID0, PMIC-CRDG2) is self-contained in DSDT; only gate is `_STA: If (SCSS==1) 0x0F else 0`. SCSS is External/undefined -> PMIC hidden (_STA=0) -> not bound -> dummy rails + no MCLK + disabled link.
- DSDT _CRS (I2C 0x4c), CLDB (type2), _DSM (0x02004C0B, 0x0B=POWER_ENABLE) all defined.
- Windows scan (20260819_dmesg_ipu3): control_logic_type=1, mclk=19.2MHz, port0, card_sku=32.
- Caveat: SCSS likely EC-gated (Windows SkcController.sys drives EC; no Linux equiv) → real fix may be an EC/camera-controller driver, not just ACPI. Board data vendor-specific.
- PLAN: boot Windows (dual-boot exists) to dump ACPI tables + run dump_intel_ipu_data for ground-truth power config + board data. Reboot needs explicit user go-ahead. Full write-up: _driver-case-live-status.md §Update 2026-09-08.
