#!/bin/bash
# Restart syndaemon for touchpad input
# This script runs as the user (via systemd user service)
# systemd handles the execution context

# Kill existing syndaemon
killall syndaemon 2>/dev/null

# Restart syndaemon as the user
syndaemon -i 1.2 -K -R -d
